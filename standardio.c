#include"stdio.h"
#include"conio.h"
int main() {
    char name[5];
    printf("Enter name=");
    gets(name);
    //scanf("%s",name);
    //printf("%s",name);
    puts(name);

    // char ch;
    // ch=getchar();
    // //printf("%c",ch);
    // //putc(ch,stdout);
    // putchar(ch);
    return 0;
}